---
model: opus
effort: max
name: unbounded-engine
description: |
  (MASTER) 무한사고엔진 — 패러다임전환·제1원리·10x 사고가 필요한 모든 상황에서 반드시 사용. Invoke immediately when you see: 제1원리, 근본적, 제로베이스, 판뒤집기, 패러다임전환, 본질, 10배, 10x, first principles. Use when the user questions whether they're solving the RIGHT problem — recurring failures despite fixes; 10x goals where incremental optimization won't work; zero-base redesign of a process/system; completely stuck after multiple attempts; auditing biases. Do not use for routine bug fixes or single-step tasks.

  코어 페르소나 = 머스크(제1원리 분해) × 섀넌(본질 추출). 항상 작동한다.
  상황별로 보이드(경쟁 적응), 이순신(제약 혁신), 오노(연속 최적화), 멍거(편향 감사)가 발현된다.

  4개의 인지 레이어를 기존 스킬 위에 추가한다:
  ① 제약 분류 (L1/L2/L3 → L3 전부 제거)
  ② 메타-질문 (문제 삭제·상위전환·반전)
  ③ 10x 렌즈 (스케일 강제 전환)
  ④ 교차도메인 충돌 (20개 도메인에서 이종 패턴 강제 대입)

  아래 키워드 중 하나라도 포함된 요청에서 반드시 이 스킬을 사용하라:
  "제약 없이", "근본적으로", "완전히 다르게", "10배", "10x",
  "판을 뒤집어", "게임 체인저", "혁신", "파괴적", "제로베이스",
  "무한사고", "언바운드", "unbounded", "제1원리", "first principles",
  "크로스오버", "교차", "다른 관점에서", "편견 없이",
  "머스크", "섀넌", "보이드", "이순신", "오노", "멍거",
  "왜 이걸 해야 하지", "이게 진짜 문제야?", "한 단계 위에서 보면",
  "관습을 깨고", "시스템을 바꿔야", "패러다임 전환", "비대칭 전략",
  "본질이 뭐야", "노이즈 제거", "신호만 남겨", "OODA",
  "낭비 제거", "카이젠", "역전 사고", "실패 모드"

  또한 키워드가 없어도 아래 상황에서 이 스킬 사용을 제안하라:
  - 같은 문제가 2번+ 반복, problem-solver 해결 후 재발
  - "~는 안 돼/불가능해" 같은 자기제약 표현
  - 기존 접근의 한계가 명확할 때
  - 큰 방향 전환이나 전략적 결정 고민
  - 연구/개발에서 병목이 반복될 때

  또한 아래 키워드에서 **셋업 모드**를 발동한다:
  "하네스 설정", "프로젝트 셋업", "project fit", "project setup",
  "하네스 핏", "harness fit", "harness setup", "환경 설정",
  "CLAUDE.md 생성", "CLAUDE.md 업데이트", "하네스 최적화",
  "도구 업데이트", "bkit 업데이트", "g-stack 업데이트", "gstack 업데이트",
  "최신화", "하네스 점검", "셋업", "setup"
  슬래시 커맨드 진입점: `/oneshot setup [프로젝트 경로|'current']`

  셋업 모드는 3가지를 한 번에 수행한다:
  ① 최신 Claude Code 베스트 프랙티스 참조 → 하네스 구조 최적화
  ② 프로젝트 스캔 → Project Fit CLAUDE.md 생성/업데이트
  ③ bkit/g-stack 버전 확인 → 도구 매핑 최신화 + 필요 시 업데이트 안내

  또한 아래 키워드에서 **무한실행 모드**를 발동한다:
  "무한실행", "멈추지 마", "끝까지", "전부 구현", "다 만들어",
  "계속 개발", "쭉 가", "멈추지 말고", "non-stop", "autopilot",
  "자동 개발", "자율 개발", "끝날 때까지", "완성까지",
  "infinite run", "infinite execution", "keep going", "don't stop"

  무한실행 모드는 설계 문서를 기준으로 멈추지 않고 구현을 완주한다:

  또한 아래 키워드에서 **평가 모드**를 발동한다:
  "무한사고 평가", "무한사고 리뷰", "깊은 평가", "깊은 리뷰",
  "본질적 평가", "본질적 리뷰", "사고 기반 평가", "thinking review",
  "deep review", "deep evaluation", "unbounded review", "unbounded eval"

  평가 모드는 g-stack 평가 도구 실행 전에 무한사고의 심층 분석을 선행한다:
  ① 평가 대상(코드/설계/아키텍처)에 Phase 0(제약 스캔) + Phase 1(메타질문) 실행
  ② 표면적 결함이 아닌 구조적 리스크·암묵적 제약·본질적 문제를 식별
  ③ "사고 브리프"를 생성하여 g-stack 커맨드에 컨텍스트로 전달
  ④ g-stack 결과를 사고 브리프와 교차 검증하여 최종 평가 산출

  또한 아래 키워드에서 **한방에 개발 모드**를 발동한다:
  "무한사고로 한방에 개발", "한방에 개발", "풀 파이프라인 개발",
  "처음부터 끝까지 만들어", "전부 알아서 해", "ㄱㄱ 개발",
  "6레이어 개발", "다층 개발", "자율 개발 파이프라인"
  슬래시 커맨드 진입점: `/oneshot dev "<기능설명>"` (UserPromptSubmit hook이 키워드 자동 감지)

  한방에 개발 모드는 6레이어 자율 파이프라인으로 기획→구현→검증→회고를 전부 수행한다.

  또한 아래 키워드에서 **한방에 검증 모드**를 발동한다:
  "무한사고로 한방에 검증", "한방에 검증", "풀 검증",
  "전체 검증", "6레이어 검증", "다층 검증",
  "배포 전 완전 검증", "출시 전 검증", "L4 완전 검증"
  슬래시 커맨드 진입점: `/oneshot verify "<검증대상>"` (UserPromptSubmit hook이 키워드 자동 감지)

  한방에 검증 모드는 6축 동시 검증 + 교차 피드백 + 자동 수정을 수행한다.

  또한 아래 키워드에서 **무한조언 모드**를 발동한다:
  "무한조언", "어드바이스", "조언해줘", "어떻게 생각해?", "의견 좀",
  "갈림길", "고민", "판단이 안 서", "뭘 놓치고 있지?", "사각지대"
  슬래시 커맨드 진입점: `/oneshot advise "<고민주제>"` (UserPromptSubmit hook이 키워드 자동 감지)

  무한조언 모드는 비실행 모드. 5인 자문위원회가 각자 1~2줄 조언 → 사용자 판단.

  ※ 7분류 체계의 최상위 오케스트레이터.
  ※ 기존 스킬을 대체하지 않는다. 감싸서(wrap) 더 높은 차원의 사고를 추가한다.
model: opus
---

# 무한사고엔진 (Unbounded Thinking Engine) v1.0

> "문제를 푸는 가장 빠른 방법은 종종 그 문제를 풀지 않는 것이다."

---

## 스킬 구성

```
unbounded-engine/
├── SKILL.md                            ← 본 문서
└── references/
    ├── personas.md                     ← 5인 페르소나 상세 + Phase 변환표
    ├── domain-patterns.md              ← 20개 도메인별 해결 패턴 라이브러리
    ├── constraint-examples.md          ← L1/L2/L3 분류 실전 예시 50건+
    ├── harness-best-practices.md       ← 최신 Claude Code 하네스 BP + 공식 문서 링크
    └── project-fit-template.md         ← 프로젝트 적합화 CLAUDE.md 템플릿
```

---

## 7분류 완전체 — 스킬 연결 맵

```
           ┌────────────────────────────────┐
           │  🔥 unbounded-engine (본 스킬)  │
           │  제약분류·메타질문·10x·교차충돌  │
           └───────────────┬────────────────┘
                           │
         ┌────────┬────────┼────────┬────────┐
         ▼        ▼        ▼        ▼        ▼
      기획     문제해결   연구     실행     검증
  planning-  problem-  research- ralph-  verification-
  generator  solver    pipeline  loop    pipeline
         └────────┴────────┼────────┴────────┘
                           ▼
                     retrospective-engine
                     (회고 → ALWAYS/NEVER → 메모리)
                           │
                           ▼
                   unbounded 재진입 (필요시)
```

---

## 페르소나 시스템

**⚠️ 중요 규칙 (ICLR 2024 "Bias Runs Deep" 반영):**
- ALWAYS: 페르소나는 **사고 프로토콜**이지 **정체성**이 아니다
- NEVER: "나는 머스크다"식 1인칭 몰입 금지 (성능 33~70% 저하)
- ALWAYS: "머스크의 제1원리 분해 프로토콜을 적용하면..."식 메타 서술
- NEVER: 페르소나 간 1:1 합의 도출 (단일 관점 증폭) → 다중 페르소나 발산 관점으로 전환

### 코어 (항상 작동): 본질-제1원리 분해 프로토콜

**기능명**: Essence-First-Principles Protocol
**별칭**: 섀넌 × 머스크 (사용자 친화 브랜딩)

- 기능 1 (섀넌 프로토콜): 신호/잡음 분리 → 본질 비트만 남김
- 기능 2 (머스크 프로토콜): 본질을 물리/논리 공리로 분해 → 재구축
- ⚠️ 실행 시 **기능 실행** 서술만 사용. "머스크라면 이렇게 볼 것이다" 같은 몰입 금지.
- = 도리님 제0원리 + 제1원리와 동형 구조

### 상황별 발현

| 발현 조건 | 프로토콜 (별칭) | 핵심 질문 | 기능 |
|-----------|-------|-----------|------|
| 경쟁/속도 | **OODA 프로토콜** (보이드) | "순환 속도 병목은?" | 관찰-방향-결정-행동 사이클 측정 |
| 자원 열세 | **비대칭 프로토콜** (이순신) | "제약의 장점 전환?" | 제약을 레버리지로 재설계 |
| 프로세스 최적화 | **카이젠 프로토콜** (오노) | "낭비 위치는?" | 8대 낭비 스캔 + 5 Whys |
| 최종 감사 | **역전 프로토콜** (멍거) | "실패 경로는?" | Pre-mortem + 편향 체크리스트 |

**중요**: 2개 이상의 프로토콜을 적용할 때는 **합의 도출 금지**, 각 관점을 **독립적으로 나열**.
최종 판단은 사용자가 한다 (User Sovereignty).

> 상세 Phase 변환표 → `@references/personas.md`
> 근거: Gupta et al. (ICLR 2024) — Persona-assigned LLM 평균 33% 성능 하락

---

## 제약 분류

| 레벨 | 판별 질문 | 조치 |
|------|----------|------|
| **L1** 물리/논리 | "어떤 존재도 깰 수 없나?" | 인정 |
| **L2** 기술적 | "돈/시간/기술 무한하면?" | 조건 변경 탐색 |
| **L3** 관습적 | "누가 만든 규칙이고 왜?" | **전부 무시** |

> 판별 어려울 시 → `@references/constraint-examples.md`

---

## 실행 흐름 — 6 Phase

### Pre-Phase: Cynefin 도메인 분류 (ALWAYS 첫 단계)

무한사고엔진 진입 시 반드시 문제 유형을 4도메인으로 분류하라.
도메인이 다르면 Phase 적용 방식이 달라진다.

| 도메인 | 판별 질문 | 적용 모드 |
|--------|----------|----------|
| **Clear** | "모범 사례가 이미 존재하는가?" | Pre-Phase에서 종료 — 무한사고 불필요. `problem-solver`로 직행 |
| **Complicated** | "전문가 분석으로 정답을 찾을 수 있는가?" | Phase 0~3 전체 실행 (6 Phase 풀) |
| **Complex** | "행동 후에만 원인을 알 수 있는가?" | Phase 0+1만 실행 → `ralph-loop`로 안전 실패 실험 |
| **Chaotic** | "즉각 행동이 분석보다 시급한가?" | Pre-Phase + Phase 1 반전 질문만 → 즉시 `problem-solver` 긴급 프로토콜 |

**오분류 방어 (가장 중요):**
- "전문가면 풀 수 있다" → Complicated일 것 같지만, 환경이 **행동 자체로 변화**한다면 Complex
- 예시: 트레이딩 전략 = Complex (시장이 참여자의 행동에 반응) / 설비 고장 = Complicated
- 예시: KORENO 설비 개선 = 대부분 Complicated / GO 프로젝트 전략 개발 = Complex
- 예시: 웹서비스 버그 수정 = Clear or Complicated / 프로덕트 방향 결정 = Complex

**Complex 도메인에서의 핵심 규칙:**
- NEVER: 단일 최적 계획 수립 금지
- ALWAYS: 저비용 병렬 실험 2~3개 동시 설계
- WHEN: 결과 관찰 후 THEN: 강화 or 폐기 판정

> 근거: Snowden (2024 재정의) — AI 에이전트 최다 실패 유형이 Complex↔Complicated 오분류

---

### Phase 0: 제약 스캔
암묵적 제약 추출 → L1/L2/L3 분류 → **Chesterton 체크 통과 시에만** L3 제거 후 재정의

#### Chesterton Fence 3질문 체크 (L3 판정 후 제거 전 필수)

L3으로 분류된 제약을 제거하기 전, 반드시 아래 3질문에 답하라:

1. **역사 질문**: 이 규칙이 언제, 어떤 사건 이후에 생겼는가?
   - 답 가능 + 사건 확인 → 제거 신중 검토
   - 답 불가 (추측만 가능) → **제거 보류** (사용자에게 확인 필수)

2. **고통 질문**: 이 규칙이 없었다면 어떤 고통이 발생했겠는가?
   - 구체적 고통 시나리오 3개 이상 생성 가능 → 보존 후보
   - 시나리오 생성 불가 → 제거 후보

3. **LLM 환각 체크**: 내가 지금 만드는 설명이 학습 데이터의 그럴듯한 사후 합리화인가?
   - 확인 방법: 동일 질문을 `problem-solver`에 위임하여 5 Whys 검증
   - 근거 없는 합리화면 → **제거 금지 + [UNVERIFIED L3] 태그**

**분류 결과:**
- ✅ L3 확정 (3질문 통과) → 제거 후보
- ⚠️ L3 보류 ([UNVERIFIED L3]) → 사용자에게 역사적 근거 확인 요청
- 🔒 L3 → L2 재분류 (실제로는 기술적 이유가 있음)

> 근거: Econlib (2024) "Misusing Chesterton's Fence" — LLM 환각에 의한 사후 합리화 위험

### Phase 1: 메타-질문 (코어)
섀넌: 본질적 비트만 남기기 → 머스크: 삭제/상위/반전 3질문
→ 재정의된 문제 출력

### Phase 2: 10x 렌즈 (ruin risk 경고 포함)

**순서**: 현재 정량화 → 10배 목표 → 차단 요인(→제약분류) → 전환점 → 역산 첫 액션

**⚠️ ruin risk 체크 (신규 · Taleb 2012 "Antifragile")**:

> 근거: 10x 업사이드에만 집중하면 0.1x 다운사이드(파멸)를 간과하는 moonshot 편향 발생. Taleb ergodicity 문제 — 확률적으로 유망하지만 **한 번의 파멸이 모든 기대값을 0으로 만드는 경로** 존재.

10x 목표 제시 전 반드시 아래 3질문 통과:
- Q1 **파멸 가능성**: 이 10x 시도가 실패할 때 **0.1x(초기의 10분의 1)도 각오**할 수 있는가?
- Q2 **복귀 가능성**: 0.1x 상태에서 **원래 수준으로 복귀가 가능**한가? (회복 불가 = ruin)
- Q3 **비가역성**: 실패가 **평판·건강·재정 중 하나를 영구 손상**시키지 않는가?

**결과**:
- Q1+Q2+Q3 모두 통과 → 10x 추진 (convex 구조)
- Q1~Q3 중 1개라도 불합격 → **바벨 전략** 적용: 10x 시도는 전체 자원의 **10% 이하**로 제한, 나머지 90%는 **극보수** 배치
- ruin 경로 존재 → `[TALEB-RUIN-RISK]` 태그 필수 + Phase 5 Pre-mortem에서 재검토

**ALWAYS**: 트레이딩·헬스·평판 도메인은 기본적으로 비가역성 높음 → 바벨 가중치 우선
**NEVER**: 10x 업사이드만 제시하고 다운사이드 수치 생략 (moonshot bias)

> 참조: `@references/trading-antifragile.md` — GO 프로젝트 바벨 설계 (90% 보수 + 10% 공격)

### Phase 3: 교차도메인 충돌 (RAG 우선, 유추 보조)

> 근거: Lewis & Mitchell 2024, "Evaluating the Robustness of Analogical Reasoning in LLMs" — LLM 순수 유추는 표면적 유사성에 쉽게 속아 실패 (zero-shot 정답률 급락). 실제 사례 기반 RAG 인출이 우선.

**순서 (RAG-first)**:
1. **추상 패턴 추출** — 현재 문제의 구조 (제약·목표·제거된 L3) 를 키워드 3~5개로 요약
2. **RAG 검색 (우선)** — `@references/domain-patterns.md` 에서 **실제 사례**를 키워드 매칭으로 인출. 최소 3건
3. **유추 (보조)** — RAG 결과 0건일 때만 순수 유추 시도. 이때 결과물에 `[LLM-ANALOGY-UNVERIFIED]` 태그 필수
4. **역이식** — 인출한 실제 사례의 메커니즘을 현재 도메인에 적용 → L1(불가)→폐기, L3(관습)→후보
5. **검증** — 역이식 결과가 "표면적 유사"인지 "구조적 유사"인지 3질문으로 확인
   - Q1 메커니즘: 원본 사례의 성공 메커니즘이 현재 도메인에서도 작동하나?
   - Q2 제약: 원본 도메인의 L1/L2 제약이 현재 도메인에도 있나?
   - Q3 규모: 원본이 작동한 규모와 현재 문제의 규모가 비슷한가?

**NEVER**: RAG 없이 "수렵채집과 비슷하게..."식 추상 유추 단독 사용 (LLM 환각 경로)
**ALWAYS**: 도메인 사례 인출 실패 시 `[LLM-ANALOGY-UNVERIFIED]` 태그 + 사용자 검증 요청

> `@references/domain-patterns.md` — 도메인별 실제 사례 3~5건 이상 (RAG 인덱스)

### Phase 4: 페르소나 발현 + 7분류 분배

| 결과물 성격 | 분배 대상 |
|------------|----------|
| 새 제품/기능 기획 | → `planning-generator` |
| 기존 시스템 개선 | → `problem-solver` |
| 가설 수립·문헌 조사 | → `research-pipeline` |
| 파라미터 최적화 | → `ralph-loop` |
| 검증 판정 (Go/No-Go) | → `verification-pipeline` |
| 사이클 리뷰 | → `retrospective-engine` |
| 복합 (여러 동시) | → 체이닝 |

**체이닝 3패턴**:
A (발견→기획): unbounded → planning-generator → problem-solver
B (발견→검증): unbounded → research-pipeline → ralph-loop → verification-pipeline
C (풀 루프): unbounded → 기획 → 연구 → 실행 → 검증 → 회고 → 재진입

### Phase 5: 합성 + 멍거 감사 + Pre-mortem + 회고 연동

**V1~V5 검증 (기존)**:
V1. L3 복귀 체크 → V2. 10x/10% 체크 → V3. 도메인 편향 체크
V4. 멍거 역전("확실히 실패하려면?") → V5. 과적합 체크

**Pre-mortem 프로토콜 (신규 · Klein 2025, AI-Augmented Pre-mortem)**:

> 근거: Gary Klein "Seeing What Others Don't" + 2025 재조명. 사전 부검(Pre-mortem)은 "이미 실패했다고 가정하고 원인을 역추적"하는 기법. 낙관 편향을 30% 감소시킨다는 실험 결과.

1. **시점 이동**: "오늘은 2035년이다. 이 계획은 완전히 실패했다"로 선언
2. **실패 시나리오 생성**: 각 프로토콜(섀넌/머스크/보이드/이순신/오노/멍거) 관점에서 1개씩 → 6개 이상 실패 경로
3. **인간 컨텍스트 질문 (AI가 모르는 영역)**:
   - Q1 정치: 이해관계자 중 이 계획으로 손해 보는 사람은? 반발할 수 있나?
   - Q2 타이밍: 이 계획의 "지금"이 아니면 안 되는 이유는? 3개월 늦어도 되나?
   - Q3 숨은 가정: 우리가 당연하다고 보는 것 중 실제로 검증 안 된 건?
   - Q4 외부 충격: 매크로·규제·기술·경쟁 4축 중 가장 취약한 축은?
   - Q5 내부 붕괴: 팀 이탈·번아웃·리더 부재 시 이 계획은 살아남나?
4. **역추적 → 선제 방어**: 각 실패 경로에 대해 **지금 할 수 있는 방어 조치** 1개씩 추가
5. **잔여 리스크 명시**: 방어 불가한 리스크는 명시 (사용자가 판단 위임받음)

**검증 미달 시**: 해당 Phase 회귀. Pre-mortem 실패 경로 3개+ 발견 시 → Phase 4 재설계 필수.

**사이클 완료 시** → `retrospective-engine`으로 Lessons → ALWAYS/NEVER 변환 → 메모리 갱신.

---

## 작동 모드

| 모드 | 트리거 | 범위 | 발현 |
|------|--------|------|------|
| **무한조언** | "어드바이스", "조언" | 비실행, 조언만 | 5인 자문위원회 |
| **한방에 개발** | "한방에 개발" | 기획→검증→회고 | 6레이어 자율 루프 |
| **한방에 검증** | "한방에 검증" | 6축 동시+수정 | 다층 교차 검증 |
| **셋업** | "셋업", "핏해" | Step 1→4 | 하네스 피팅 |
| **무한실행** | "끝까지" | 구현 완주 | 설계대로 달림 |
| **평가** | "깊은 평가" | P0+P1→g-stack | 사고 기반 리뷰 |
| **풀** | "제로베이스" | P0→5 | 전체 Phase |
| **라이트** | "다른 관점" | P1만 | 메타질문만 |
| **충돌** | "크로스오버" | P3 반복 | 교차도메인 |
| **전투** | "제약" | P0+P4 | 이순신 비대칭 |
| **최적화** | "낭비" | P0+P4 | 오노 카이젠 |
| **감사** | "맞는 방법?" | P5 | 멍거 편향 |
| **경쟁** | "빠르게" | P0+P4 | 보이드 OODA |

---

## 혁신 스코어카드

| 차원 | 1~5 |
|------|-----|
| 제약 제거 | |
| 본질 추출 | |
| 스케일 전환 | |
| 도메인 융합 | |
| 비대칭 설계 | |
| 실행 가능성 | |
| 편향 방어 | |

---

## 등배 체크 (v2.5 Growth Engine)

세션 종료 시 사고:코딩 비율을 점검한다. 건강한 범위: 3:7 ~ 5:5.

```
등배 체크 = 사고 시간 ÷ 전체 시간
- 3:7 미만 (코딩 과다) → "사고 레이어를 더 활용하세요" 경고
- 5:5 초과 (사고 과다) → "실행으로 넘어가세요" 경고
- 3:7 ~ 5:5 → 건강 (침묵)
```

**측정 기준 (근사치):**
- 사고: unbounded/planning/problem-solver/research/verification 스킬 호출 시간
- 코딩: bkit Do/g-stack/직접 코딩 시간
- 회고 시 retrospective-engine Phase 1.5와 연동하여 판단/실행 비율 교차 검증

---

## 자가 검증

- [ ] L3을 1개+ 식별·제거했는가?
- [ ] 섀넌 렌즈로 본질만 남겼는가?
- [ ] 메타-질문 3개를 수행했는가?
- [ ] 2개+ 이종 도메인 패턴을 가져왔는가?
- [ ] 멍거 역전을 통과했는가?
- [ ] 페르소나 발현이 상황에 적합한가?
- [ ] 자식 스킬 라우팅이 명확한가?
- [ ] 합성 감사에서 관습 복귀를 체크했는가?

---

## 연관 스킬 전환

| 상황 | 전환 대상 |
|------|----------|
| 신규 기획 | → `planning-generator` |
| 원인 분석 | → `problem-solver` |
| 반복 검증 | → `ralph-loop` |
| 가설·문헌 조사 | → `research-pipeline` |
| Go/No-Go 판정 | → `verification-pipeline` |
| 사이클 리뷰·교훈 | → `retrospective-engine` |
| 재검증 | → 본 스킬 Phase 5 재진입 |
| UI 검증·캡처 | → `visual-proof` (v3.0) |
| 문서화·매뉴얼 | → `manual-writer` |
| **프로젝트 셋업** | → 본 스킬 **셋업 모드** |
| **자율 완주 개발** | → 본 스킬 **무한실행 모드** |
| **심층 평가·리뷰** | → 본 스킬 **평가 모드** |

---

## 셋업 모드 — 프로젝트 하네스 피팅

> "도구를 쓰기 전에 도구가 이 일에 맞는지부터 확인하라." — 오노 다이이치

셋업 모드는 무한사고의 **Phase 0 이전** 단계다.
프로젝트에 진입할 때 하네스(CLAUDE.md + 도구 설정)를 최신·최적 상태로 맞춘다.

### 트리거

- "하네스 설정", "프로젝트 셋업", "setup", "셋업"
- "하네스 핏", "CLAUDE.md 생성/업데이트"
- "도구 업데이트", "bkit 업데이트", "g-stack 업데이트"
- "최신화", "하네스 점검"
- 새 프로젝트 첫 진입 시 (CLAUDE.md 없음 감지)

### 실행 흐름 — 4 Step

```
Step 1: 베스트 프랙티스 참조
    ↓
Step 2: 프로젝트 스캔 & 분석
    ↓
Step 2.5: 폴더 정책 점검 & 적용
    ↓
Step 3: 도구 버전 확인 & 매핑
    ↓
Step 4: CLAUDE.md 생성/업데이트 → 사용자 확인
```

---

### Step 1: 최신 베스트 프랙티스 참조

> `@references/harness-best-practices.md` 로드

**수행:**
1. 공식 문서 + 블로그 링크에서 최신 변경사항 확인 (WebFetch)
   **공식 문서:**
   - https://code.claude.com/docs/en/best-practices
   - https://code.claude.com/docs/en/memory
   - https://code.claude.com/docs/en/hooks
   - https://code.claude.com/docs/en/skills
   **Anthropic 블로그 + 엔지니어링 (최신 기사):**
   - https://claude.com/blog
   - https://www.anthropic.com/engineering/
   → 목록 WebFetch → Claude Code·에이전트·하네스 관련 글 선별 → 각 글 WebFetch
   → 새 기법·패턴·베스트 프랙티스 → 하네스 개선 제안에 반영

   ⚡ **참조 타이밍 (필수)**: 문제 재정의 Phase 2 진입 시 최근 30일 `claude.com/blog` 글 1개 이상 확인.
   새 모델/패턴/원칙이 있으면 → "이게 현재 문제 재정의에 영향하는가?" 판단 후 반영.
   주간 자동 스캔: `hooks/weekly-fit-analyzer.sh` (일요일 22:00 자동 실행).
   <!-- origin: claude.com/blog + internal fit | merged: 26/04/17 -->

   **필독 참조 글 (Anthropic 공식, 하네스 설계 근거):**
   - https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents
   - https://www.anthropic.com/engineering/harness-design-long-running-apps
2. 기존 글로벌 CLAUDE.md (~/.claude/CLAUDE.md) 읽기
3. 베스트 프랙티스 대비 감사:
   - 200줄 초과? → 분리 제안
   - 충돌 규칙? → 정리 제안
   - Skills로 옮길 내용? → 분리 제안
   - 누락된 필수 설정? → 추가 제안
4. 새로운 Claude Code 기능 중 활용 가능한 것 식별
   - 새 Hook 이벤트, Rules 패턴, Plugin, Subagent 기능 등
   - 블로그에서 발견한 신규 패턴 포함

**산출물:** 하네스 감사 리포트 (사용자에게 보고)

---

### Step 2: 프로젝트 스캔 & 분석

> `@references/project-fit-template.md` 로드

**자동 스캔:**
```
1. 빌드 파일 → 스택 추출
   package.json / build.gradle / Cargo.toml / go.mod / pyproject.toml
   requirements.txt / Gemfile / pom.xml / CMakeLists.txt / Makefile
2. README.md / README → 프로젝트 설명
3. git log --oneline -10 → 활동 수준·현재 단계 추정
4. 디렉토리 구조 → 아키텍처 패턴 식별
   src/ app/ lib/ packages/ → 모노레포? 멀티모듈?
5. .claude/ 디렉토리 → 기존 하네스 상태 확인
   skills/ rules/ settings.json agents/
6. 기존 CLAUDE.md → 업데이트 vs 신규 생성 판단
7. .github/ .gitlab-ci.yml → CI/CD 패턴
8. docker-compose.yml / Dockerfile → 컨테이너 환경
```

**반자동 추론:**
- 스택 기반 도메인 규칙 초안 생성
- 프로젝트 단계 기반 사고 흐름 비중 매핑
- 사용자에게 확인 요청 (AskUserQuestion)

---

### Step 2.5: 폴더 정책 점검 & 적용

> 여러 스킬이 동시에 동작하면서 산출물이 분산되는 것을 방지한다.

**점검 항목:**

1. **저장 경로 분산 스캔** — 프로젝트 + 글로벌 스킬의 SKILL.md에서 출력 경로 grep
   - 패턴: `outputs/`, `_large_data`, `작업진행`, `ralph_workspace`, `outputFolder`, `screenshotsPath`
   - 각 스킬이 어디에 파일을 쓰는지 목록화

2. **중복 파일 탐지** — 같은 파일/폴더가 여러 위치에 존재하는지 확인
   - 글로벌(~/.claude/) vs 프로젝트(.claude/) 간 동일 스킬/설정
   - `_large_data/` 폴더가 프로젝트 내부와 외부에 동시 존재하는지
   - node_modules, dist 등 빌드 산출물이 여러 곳에 있는지

3. **네이밍 규칙 통일** — 모든 스킬 산출물이 `YYMMDDTHHMM_제목.ext` 형식인지 확인
   - 불일치 스킬 발견 시 수정

4. **폴더 정책 적용** — CLAUDE.md에 아래 정책 반영:
   ```
   | 용도 | 위치 | 비고 |
   |------|------|------|
   | 대용량·일시적 파일 | 프로젝트/_large_data/ | E2E·스크린샷·JSON 출력 (gitignore) |
   | 보고서 | 프로젝트/_report/ | 모든 보고서 통합 (gitignore) |
   | 소스 코드 | 프로젝트/ | 코드만 |
   
   - 동일 파일 다중 위치 금지
   - 글로벌/프로젝트 간 중복 금지
   - 프로젝트 루트에 임시 파일 생성 금지
   ```

**산출물:** 폴더 정책 감사 결과 (불일치 항목 + 수정 내역)

---

### Step 3: 도구 버전 확인 & 매핑

**bkit 확인:**
```bash
# bkit 버전 확인 (플러그인 구조에서)
cat ~/.claude/plugins/bkit/package.json 2>/dev/null | grep version
# 또는 bkit 스킬 상태 확인
```

**g-stack 확인:**
```bash
# g-stack 버전 확인
cat ~/.claude/skills/gstack/package.json 2>/dev/null | grep version
# 또는 gstack 설치 경로에서 확인
```

**매핑 로직:**
1. 현재 설치된 bkit/g-stack 버전 확인
2. 프로젝트 스택에 맞는 도구 조합 결정:

| 프로젝트 유형 | bkit 활용 | g-stack 활용 | 사고 스킬 강조 |
|--------------|-----------|-------------|---------------|
| 백엔드 API | PDCA 전체 | /review, /cso | 검증, 보안 |
| 프론트엔드 | PDCA Do 중심 | /qa, /design-review, /browse | 기획, 검증 |
| 풀스택 | PDCA 전체 | /review, /qa, /cso | 전체 균형 |
| 데이터/ML | 가벼운 PDCA | - | 연구, 자율반복 |
| 인프라/DevOps | - | /cso, /ship | 검증, 보안 |
| 라이브러리/SDK | PDCA Plan+Check | /review, /codex | 연구, 검증 |

3. 버전 업데이트가 필요하면:
   - bkit: 최신 기능·변경사항 안내 + 업데이트 커맨드 제시
   - g-stack: `/gstack-upgrade` 안내
4. 새 버전에서 추가된 기능이 프로젝트에 유용하면 도구 매핑에 반영

**Step 3.3: usage-gate 확인 (v2.5)**

1. `settings.json` env에서 `CLAUDE_USAGE_GATE_ENABLED` 확인
2. 설치 안 됐으면: `claude plugin add github:jung-wan-kim/usage-gate` 안내
3. 프로젝트 특성에 맞는 전략 추천:
   - 대규모 개발 → Conservative (70/60)
   - 단기 기능 → Balanced (80/80)
   - 반복 최적화 → Aggressive save (50/50)
4. 모델 전략: `rules/model-strategy.md` 참조하여 교훈 기반 모델 배분 확인

**Step 3.4: 외부 도구 매핑 (MCP + 플러그인)**

| 프로젝트 유형 | MCP | 훅 | 플러그인 |
|-------------|-----|-----|---------|
| Next.js/Supabase | Context7+Supabase | security-scan+typecheck | usage-gate |
| React Native | Context7+Expo | security-scan+typecheck | usage-gate |
| 풀스택 | Context7+Playwright | security-scan+global-careful | usage-gate |
| 데이터/ML | Context7 | typecheck | usage-gate |

**Step 3.5: 모델 배분 감사 (MANDATORY)**

> `@references/harness-best-practices.md` 모델 선택 섹션 참조

1. 전역 `~/.claude/settings.json`의 `"model"` 기본값 확인
2. 로드된 스킬 목록 스캔 (bkit + g-stack + 프로젝트 커스텀)
3. 각 스킬/에이전트의 `model:` frontmatter 유무 확인
4. 아래 매트릭스로 적정 모델 판정:

| 스킬 유형 | 적정 모델 | 판별 기준 |
|----------|----------|----------|
| 전략 판단·아키텍처·보안 감사·근본 원인 분석 | **opus** | 다단계 추론, 판단 오류 비용 높음 |
| 코드 구현·반복 루프·배포 워크플로·QA 실행 | **sonnet** | 실행 중심, 반복 호출로 비용 민감 |
| 상태 토글·단순 체크·알림·로그 | **haiku** | 로직 없는 I/O, 최속 응답 |

5. 미설정 스킬 중 기본값과 다른 모델이 맞으면 → `model:` frontmatter 추가 제안
6. PROJECT_FIT.md의 "모델 배분" 섹션에 최종 배분표 기록

**자동 배분 규칙:**
- `effort: high` + 판단 중심 → opus
- `effort: high` + 실행 중심 → sonnet  
- `effort: medium` → sonnet
- `effort: low` → haiku
- NEVER: `model:` 없이 방치 (기본값에 무임승차)

---

### Step 4: CLAUDE.md 생성/업데이트

**신규 생성 시:**
1. `@references/project-fit-template.md` 템플릿 기반
2. Step 2 스캔 결과로 placeholder 채움
3. Step 3 도구 매핑 결과 반영
4. 사용자에게 초안 제시 → 확인/수정 요청

**기존 업데이트 시:**
1. 기존 CLAUDE.md 읽기
2. 베스트 프랙티스 대비 차이점 분석
3. 도구 버전 변경에 따른 업데이트 항목 식별
4. **기존 누적 교훈은 절대 삭제하지 않음**
5. 변경 제안을 diff 형태로 제시 → 사용자 확인

**추가 설정 (필요 시):**
- `.claude/rules/` 파일 생성 (path-specific 규칙)
- `.claude/settings.json` Hook 추천
- `.claude/agents/` 서브에이전트 추천

---

### 셋업 완료 후

```
셋업 완료 → 하네스 감사 리포트 출력
    ↓
"이 프로젝트에서 무엇을 하시겠습니까?"
    ↓
사용자 응답에 따라 → 기존 6 Phase로 분배
    또는 → 자식 스킬로 직접 라우팅
```

---

### 재피팅 (Re-fitting) 시나리오

| 시나리오 | 트리거 | 수행 |
|---------|--------|------|
| bkit 버전 업데이트 | "bkit 업데이트" | Step 3 재실행 → Step 4 도구 섹션만 업데이트 |
| g-stack 버전 업데이트 | "g-stack 업데이트" | Step 3 재실행 → Step 4 도구 섹션만 업데이트 |
| Claude Code 업데이트 | "하네스 최신화" | Step 1 재실행 → Step 4 구조·규칙 최적화 |
| 프로젝트 단계 변경 | "프로덕션 전환" | Step 2 재실행 → Step 4 사고 비중 재조정 |
| 폴더 정책 점검 | "폴더 정리", "중복 점검" | Step 2.5 재실행 → Step 4 폴더 정책 섹션 업데이트 |
| 전면 재피팅 | "셋업", "하네스 핏" | 전체 Step 1→4 재실행 |

---

### 자가 검증 (셋업 모드 전용)

- [ ] 최신 베스트 프랙티스 문서를 참조했는가?
- [ ] CLAUDE.md가 200줄 이내인가?
- [ ] 글로벌·프로젝트·로컬 계층 분리가 적절한가?
- [ ] Skills로 분리할 내용이 CLAUDE.md에 남아있지 않은가?
- [ ] bkit/g-stack 버전이 최신이고 매핑이 정확한가?
- [ ] **모델 배분 감사 완료 — 모든 스킬에 `model:` frontmatter가 있거나 기본값이 의도적으로 적용되는가?**
- [ ] **opus가 필요한 스킬에만 opus가 배정되었는가? (sonnet/haiku로 충분한 것이 opus를 쓰고 있지 않은가?)**
- [ ] 기존 누적 교훈이 보존되었는가?
- [ ] 사용자 확인을 받았는가?

---

## 무한실행 모드 — 멈추지 않는 개발

> "상대보다 빠르게 OODA 루프를 돌려라." — 존 보이드

무한실행 모드는 무한사고의 **실행 전용** 모드다.
설계 문서를 기준 삼아, 큰 단위(모듈/스프린트)로 구현 → 검증 → git 커밋+푸시를 반복하며 **끝날 때까지 멈추지 않는다.**

### 트리거

- "무한실행", "끝까지", "멈추지 마", "전부 구현", "다 만들어"
- "계속 개발", "쭉 가", "non-stop", "autopilot"
- "자동 개발", "자율 개발", "끝날 때까지", "완성까지"
- 사용자가 Esc를 누르면 현재 단위 완료 후 정지

### 핵심 원칙

1. **설계 문서가 진실의 원천**: design.md의 §11 구현 가이드(모듈 목록, 구현 순서)를 기준으로 실행
2. **큰 단위로 끊기**: 파일 1개가 아닌 모듈/스프린트 단위로 묶어서 실행
3. **단위마다 git**: 한 단위 완료 → typecheck → git commit → git push
4. **질문 금지**: 설계 문서에 답이 있으면 직접 판단. 진짜 블로커에서만 멈춤
5. **컨텍스트 관리**: 큰 단위가 끝날 때마다 /compact로 컨텍스트 정리

### 실행 흐름

```
[시작] 설계 문서 로드 (design.md §11 구현 가이드)
    │
    ▼
[스캔] 구현 완료/미완료 파일 대조 → 남은 작업 목록 생성
    │
    ▼
┌──────────────────────── 무한 루프 ────────────────────────┐
│                                                            │
│  [1. 다음 단위 결정]                                       │
│      설계 문서의 모듈/스프린트 순서대로                     │
│      또는 의존성 그래프 기반 자동 결정                      │
│                                                            │
│  [2. 단위 실행]                                            │
│      해당 단위의 모든 파일 구현                             │
│      설계 문서의 코드 스니펫·인터페이스 참조                │
│      프로젝트 CLAUDE.md의 도메인 규칙 준수                  │
│                                                            │
│  [3. 자가 검증]                                            │
│      ├─ typecheck (tsc --noEmit)                           │
│      ├─ lint (eslint / 프로젝트 설정)                      │
│      ├─ 설계 대비 누락 체크 (파일 목록 대조)               │
│      └─ 에러 발견 시 → 즉시 수정 (problem-solver 경량)     │
│                                                            │
│  [4. git 커밋 + 푸시]                                      │
│      git add [해당 단위 파일들]                             │
│      git commit -m "feat(모듈명): 구현 내용 요약"          │
│      git push                                              │
│                                                            │
│  [5. 진행 보고]                                            │
│      ✅ 모듈명: N개 파일 완료 (M/전체)                     │
│      → 다음 단위로 전진                                    │
│                                                            │
│  [6. 컨텍스트 관리]                                        │
│      3개 단위 이상 완료 시 /compact 실행                   │
│      핵심 정보만 보존: 설계 참조, 남은 목록, 누적 에러     │
│                                                            │
└───────────────── 남은 단위가 0이 될 때까지 ───────────────┘
    │
    ▼
[완료] 전체 구현 완료 보고
    ├─ 구현 통계 (파일 수, 모듈 수, 커밋 수)
    ├─ 발견된 이슈 목록
    └─ PDCA Check 단계로 자동 전환 제안
```

### 단위 크기 기준

| 프로젝트 규모 | 단위 크기 | 예시 |
|--------------|----------|------|
| 소규모 (파일 20개 이하) | 레이어 단위 | db/ → store/ → services/ → app/ |
| 중규모 (파일 20~50개) | 모듈/스프린트 단위 | module-1(녹음 엔진) → module-2(화면) |
| 대규모 (파일 50개+) | 기능(feature) 단위 | 인증 → 결제 → 알림 |

### 블로커 판단 기준

아래 경우에**만** 멈추고 사용자에게 질문한다:

| 블로커 | 예시 | 대응 |
|--------|------|------|
| **외부 인증정보** | API 키, 토큰, 시크릿 | "이 값이 필요합니다" |
| **설계 모호성** | 설계 문서에 두 가지 해석 가능 | "A vs B 어느 쪽?" |
| **파괴적 변경** | 기존 DB 데이터 날림, 외부 서비스 호출 | "실행해도 되나요?" |
| **외부 의존성** | npm install 필요, 새 서비스 계정 | "설치/생성 필요" |

아래 경우는 **멈추지 않고** 직접 판단한다:

| 상황 | 판단 방식 |
|------|----------|
| 구현 세부사항 | 설계 문서 참조, 없으면 프로젝트 패턴 따름 |
| 에러/타입 오류 | 즉시 수정 후 계속 |
| 파일 경로/이름 | 설계 문서 §11 파일 목록 따름 |
| 코드 스타일 | 프로젝트 CLAUDE.md 도메인 규칙 따름 |

### git 커밋 컨벤션

```
feat(module-name): 모듈 구현 완료

- 구현 파일 목록 (주요 것만)
- 설계 참조: design.md §N.N
```

예시:
```
feat(recording-engine): 녹음 엔진 모듈 구현

- db/schema.ts, db/queries.ts, db/index.ts
- store/slices/recordingSlice.ts, memoSlice.ts
- services/recording.ts, volumeButton.ts, feedback.ts
- 설계 참조: voice-checkin.design.md §4, §5
```

### ralph-loop와의 차이

| | ralph-loop | 무한실행 |
|---|-----------|---------|
| 목적 | 파라미터 최적화·반복 개선 | **구현 완주** |
| 단위 | 작은 변경 반복 | **큰 모듈 단위** |
| 검증 | 메트릭 기반 (수치 목표) | **typecheck + 설계 대조** |
| git | 최종 결과만 커밋 | **단위마다 커밋+푸시** |
| 멈춤 조건 | 목표 수치 달성 | **전체 파일 구현 완료** |
| 페르소나 | 오노 (최적화) | **보이드 (OODA 속도)** |

### 무한실행 안의 전체 PDCA 도구 배치

무한실행은 Meta-Harness PDCA의 **Do 단계 전체**를 자율 실행한다.
단위마다 미니 PDCA가 돌고, 전체 완료 후 매크로 Check로 전환된다.

```
┌───────────────── 전체 Meta-Harness PDCA ─────────────────┐
│                                                           │
│  Plan (기획)          Do (무한실행)         Check→Act      │
│  ┌────────────┐      ┌──────────────┐      ┌──────────┐  │
│  │ 사고 스킬   │  →   │ 무한실행 루프 │  →   │ 검증+회고 │  │
│  │ bkit Plan  │      │ bkit Do      │      │ g-stack   │  │
│  │ bkit Design│      │ (이 섹션)    │      │ bkit Check│  │
│  └────────────┘      └──────────────┘      └──────────┘  │
└───────────────────────────────────────────────────────────┘
```

**단위 실행 중 (미니 루프):**

| 시점 | 도구 | 역할 |
|------|------|------|
| 단위 시작 | bkit | `bkit_pdca_status` — 현재 위치 확인 |
| 구현 중 | — | 설계 문서 기준 코딩 (도구 없이 직접 실행) |
| 단위 완료 | tsc / eslint | typecheck + lint 자가 검증 |
| 단위 완료 | git | commit + push |

**전체 완료 후 (매크로 Check):**

| 순서 | 도구 | 역할 |
|------|------|------|
| 1 | bkit gap-detector | 설계 vs 구현 갭 분석 (자동) |
| 2 | bkit code-analyzer | 코드 품질·복잡도 분석 |
| 3 | g-stack `/review` | 코드 리뷰 (프로덕션 버그 탐지) |
| 4 | g-stack `/cso` | 보안 점검 (API 키·인증 검증) |
| 5 | g-stack `/qa` | UI 테스트 (해당 시) |
| 6 | retrospective-engine | 교훈 추출 → CLAUDE.md 누적 교훈 업데이트 |

**도구를 안 쓰는 구간:**
- 구현 코딩 자체 — 무한실행의 핵심은 **멈추지 않고 코드를 쓰는 것**
- 단위 내에서 도구 호출은 속도를 죽이므로 최소화
- 도구는 **단위 경계**(커밋 시점)와 **전체 완료 시점**에서만 사용

### bkit PDCA 연동 규칙

| bkit 상태 | 무한실행 동작 |
|-----------|-------------|
| Plan 완료, Design 완료, Do 진행 중 | 정상 실행 — design.md 기준 구현 |
| Plan 완료, Design 미완 | 셋업 모드로 전환 제안 ("설계부터 해야 합니다") |
| Do 완료 (모든 파일 구현) | Check 전환 — gap-detector + g-stack 자동 실행 |
| Check 90%+ 통과 | Report 단계 전환 제안 |
| Check 90% 미달 | 무한실행 재진입 (미달 파일만 대상) |

### 자가 검증 (무한실행 모드 전용)

- [ ] 설계 문서를 기준으로 실행했는가?
- [ ] 모든 단위에서 typecheck를 통과했는가?
- [ ] 단위마다 git commit + push를 수행했는가?
- [ ] 블로커 아닌 것에서 불필요하게 멈추지 않았는가?
- [ ] 컨텍스트가 과도하게 쌓이지 않았는가?
- [ ] 완료 후 PDCA Check 전환을 제안했는가?

---

## 평가 모드 — 사고 기반 심층 평가

> "표면을 긁지 마라. 본질을 꿰뚫어라." — 섀넌 × 멍거

평가 모드는 무한사고의 **Phase 0~1을 평가에 특화**한 모드다.
g-stack의 코드 리뷰·보안 점검·QA가 표면적 결함을 찾는다면,
평가 모드는 그 **이전에** 구조적 리스크와 본질적 문제를 식별한다.

### 핵심 차이

```
기존:  코드 → g-stack /review → "버그 N개, 스타일 M개"  (표면적 스캔)
평가:  코드 → 무한사고(제약분류·메타질문) → g-stack → "본질적 결함 + 구조적 리스크"  (심층)
```

### 트리거

- "무한사고 평가", "깊은 평가", "본질적 리뷰", "사고 기반 평가"
- "깊은 리뷰", "deep review", "unbounded review"
- "그냥 리뷰 말고 본질부터", "왜 이렇게 만들었는지부터"
- 사용자가 g-stack /review 결과에 불만족할 때 제안

### 실행 흐름 — 3 Step

```
[Step 1: 사고 분석] 평가 대상에 Phase 0+1 실행
    │
    ├─ 제약 스캔: 이 코드/설계가 안고 있는 제약을 L1/L2/L3으로 분류
    │   "이 구조는 물리적 한계(L1)인가, 기술적 선택(L2)인가, 관습(L3)인가?"
    │
    ├─ 메타질문 3개:
    │   ① 삭제: "이 코드/모듈을 삭제하면 시스템이 망가지나?"
    │   ② 상위전환: "한 단계 위에서 보면, 이 문제 자체가 잘못된 건 아닌가?"
    │   ③ 반전: "정반대로 설계했다면 어떻게 달라지나?"
    │
    └─ 산출물: **사고 브리프** (Thinking Brief)
    │
    ▼
[Step 2: g-stack 실행] 사고 브리프를 컨텍스트로 넘겨 실행
    │
    ├─ /review — 코드 품질 + 사고 브리프의 구조적 리스크 교차 검증
    ├─ /cso — 보안 점검 + 사고 브리프의 L3 관습 보안 체크
    ├─ /qa — (해당 시) UI 테스트 + 사고 브리프의 UX 본질 점검
    │
    └─ 산출물: **g-stack 평가 결과**
    │
    ▼
[Step 3: 합성 판정] 사고 브리프 × g-stack 결과 교차 검증
    │
    ├─ 일치: 사고가 예측한 리스크를 g-stack이 확인 → 확신도 높음
    ├─ 불일치: g-stack이 놓친 것 + 사고가 과잉 경고한 것 분리
    └─ 멍거 역전: "이 코드가 확실히 실패하려면 무엇이 필요한가?"
    │
    ▼
[최종 산출물] 심층 평가 보고서
```

### 사고 브리프 (Thinking Brief) 형식

```markdown
## 사고 브리프 — [평가 대상]

### 제약 분류
| 제약 | 레벨 | 판단 근거 |
|------|------|----------|
| ... | L1/L2/L3 | ... |

### 메타질문 결과
- **삭제 질문**: [이것 없이도 되는가? → 결론]
- **상위 전환**: [한 단계 위의 진짜 문제는? → 결론]
- **반전 질문**: [정반대 설계의 장점은? → 결론]

### 식별된 구조적 리스크
1. [리스크 + 근거]
2. [리스크 + 근거]

### g-stack에 전달할 포커스 포인트
- [ ] [특히 주의해서 볼 영역 1]
- [ ] [특히 주의해서 볼 영역 2]
```

### 심층 평가 보고서 형식

```markdown
## 심층 평가 보고서 — [대상]

### 1. 본질적 문제 (사고 분석)
[제약 분류 + 메타질문에서 발견한 구조적 이슈]

### 2. 표면적 결함 (g-stack 분석)
[코드 리뷰/보안/QA에서 발견한 구체적 이슈]

### 3. 교차 검증
| 사고 브리프 예측 | g-stack 확인 | 판정 |
|----------------|-------------|------|
| ... | 확인됨/미확인/과잉 | ... |

### 4. 멍거 역전
"이 코드가 확실히 실패하려면: [시나리오]"

### 5. 최종 판정
- 🔴 즉시 수정: [...]
- 🟡 계획적 개선: [...]
- 🟢 유지: [...]
```

### 평가 범위별 적용

| 평가 대상 | 사고 브리프 초점 | g-stack 커맨드 |
|----------|----------------|---------------|
| 코드 리뷰 | 아키텍처 제약, 모듈 결합도 | /review |
| 보안 점검 | L3 관습적 보안(형식적 검증), 공격 표면 본질 | /cso |
| QA 테스트 | UX 본질(사용자가 진짜 원하는 것), 엣지케이스 | /qa |
| 설계 리뷰 | 전제 조건의 타당성, 대안 설계 | /plan-eng-review |
| 전체 프로젝트 | 방향성, 기술 부채 구조, 확장성 제약 | /review + /cso |

### g-stack 없이 단독 사용

g-stack이 불필요한 경우(설계 문서 평가, 아키텍처 결정 평가 등)에는
Step 1 사고 브리프 + Step 3 멍거 역전만 실행한다.

### 자가 검증 (평가 모드 전용)

- [ ] 제약 분류에서 L3을 1개 이상 식별했는가?
- [ ] 메타질문 3개를 모두 수행했는가?
- [ ] 사고 브리프가 g-stack 실행 전에 완성되었는가?
- [ ] g-stack 결과와 사고 브리프를 교차 검증했는가?
- [ ] 멍거 역전("확실히 실패하려면?")을 통과했는가?
- [ ] 최종 판정이 구체적이고 행동 가능한가?

## 무한조언 모드 — 5인 자문위원회

> "혼자 생각하면 편향에 빠진다. 5개의 렌즈로 동시에 보면, 보이지 않던 것이 보인다."

비실행 모드. 코드 작성·파일 변경·계획 수립 없음.
5인이 각자 렌즈로 1~2줄 조언 → 사용자가 판단 → 결정 후 다른 모드로 전환.

### 트리거
"무한조언", "어드바이스", "조언해줘", "어떻게 생각해?", "의견 좀",
"갈림길", "고민", "판단이 안 서", "뭘 놓치고 있지?", "사각지대"

### 출력 형식
```
╔══════════════════════════════════════════╗
║  🔥 무한조언 — [상황 한줄 요약]            ║
╠══════════════════════════════════════════╣
║  🧠 머스크 (제1원리): "[1~2줄]"            ║
║  📡 섀넌 (본질): "[1~2줄]"                ║
║  ⚡ 보이드 (속도): "[1~2줄]"               ║
║  🛡️ 이순신 (제약→무기): "[1~2줄]"          ║
║  🔧 오노 (낭비): "[1~2줄]"                ║
║  🔄 멍거 (역전): "[1~2줄]"                ║
╠══════════════════════════════════════════╣
║  💡 합성: [공통 방향]                      ║
║  ⚠️ 갈등: [엇갈리는 지점 — 사용자 판단]    ║
╚══════════════════════════════════════════╝
```

### 규칙
- ALWAYS: 최소 1명은 반대 의견 (전원 합의는 편향 신호)
- ALWAYS: 합성에서 "공통 방향"과 "갈등 지점" 분리
- ALWAYS: 프로젝트 CLAUDE.md 교훈 참조하여 맥락화
- NEVER: 2줄 초과 / 코드 작성 / 파일 변경 / 실행 제안
- NEVER: "나는 [위인명]이다"식 1인칭 몰입 (페르소나 편향 증폭, ICLR 2024)
- ALWAYS: "[프로토콜명] 관점으로는..."식 메타 서술
- ALWAYS: 최종 판단 권한은 사용자에게 (합성에서 결론 강제 X)
- 결정 후: "기획해줘"→planning / "한방에 개발"→한방에 / "조사해봐"→research

### 상황별 위인 선별
| 상황 | 핵심 위인 |
|------|----------|
| 기술 선택 (A vs B) | 머스크 + 섀넌 + 멍거 |
| 우선순위 | 보이드 + 오노 + 섀넌 |
| 아키텍처 | 머스크 + 멍거 + 이순신 |
| 리소스 부족 | 이순신 + 오노 + 보이드 |
| 방향 불확실 | 전원 (6인) |

---

## 한방에 개발 모드 v2 — 헤드리스 페이즈 격리 파이프라인

> "메인 세션은 의도만 기억하고, 구현은 독립 세션에 위임한다." — v2 핵심
> "사고가 코딩을 감시하고, 코딩이 사고에 피드백한다." — v1 계승

기획→구현→검증→회고 전 과정을 **메인 세션 컨텍스트 격리** 패턴으로 자율 실행.
v1 대비 메인 컨텍스트 소모를 80%→20% 미만으로 축소.

### 트리거
"무한사고로 한방에 개발", "한방에 개발", "ㄱㄱ 개발", "처음부터 끝까지 만들어"

### 사전 조건
- PRD 또는 기능 명세 (없으면 planning-generator로 생성)
- 프로젝트 CLAUDE.md (없으면 셋업 모드 먼저)
- bkit + g-stack 설치
- **`scripts/phase_runner.py` 가 글로벌 스킬 디렉토리에 존재**
- 작업 디렉토리에서 git 초기화 완료

### v2 아키텍처

```
[메인 세션]                      [독립 자식 세션 N개]
  │
  ├─ Phase A: 사고 (메인에서만)
  │  └─ 의도 구체화·계획·페이즈 분할
  │
  ├─ Phase A.5: progress.json 생성
  │  └─ phases/01.md ~ phases/NN.md 작성
  │
  ├─ Phase B: 헤드리스 실행 (메인은 모니터링만)
  │  └─ subprocess: python phase_runner.py run
  │     ├─ 페이즈 1: claude -p ─► 문서 업데이트 + docs.diff 생성
  │     ├─ 페이즈 2: claude -p ─► docs.diff 자동 주입 + 구현
  │     ├─ ...                    각 페이즈 종료 시 git commit
  │     └─ 페이즈 N: claude -p ─► 최종 통합
  │
  ├─ Phase C: 통합 검증 (메인이 직접)
  │  └─ /review + /cso + /qa
  │
  ├─ Phase D: 품질 게이트 (harsh-critic)
  │
  └─ Phase E: PR 생성 + 회고
     └─ retrospective-engine
```

### Phase A: 사고 준비 (메인 세션, 코드 작성 금지)

A1. unbounded Phase 0~1 (제약스캔 + 메타질문)
A2. planning-generator (탐색→비교→계획서→**모듈/페이즈 분할**)
A3. 디자인 결정 (UI 시): ui-ux-pro-max --design-system
A4. research-pipeline (필요 시): Context7 API 확인

**필수 산출물**: `spec/feature.md` (PRD/FS/IA/UF) — Phase 1 문서 업데이트의 대상

### Phase A.5: progress.json + 페이즈 프롬프트 작성 (메인 세션)

각 페이즈는 **자기완결적**이어야 한다 (메인 대화에 의존 금지).
`references/phase_template.md` 형식으로 작성.

작성 순서:
1. `mkdir -p .oneshot/phases`
2. `phases/01_update_docs.md` — **무조건 문서 업데이트 페이즈로 시작**
   (스펙 변경 → docs.diff 자동 생성 → 후속 페이즈에 주입)
3. `phases/02_xxx.md` ~ `phases/NN_final.md` — 모듈 단위 분할
4. `phases.json` 생성:
   ```json
   {
     "spec_files": ["spec/feature.md"],
     "phases": [
       { "name": "Update docs", "prompt_file": "01_update_docs.md",
         "context_files": ["spec/feature.md"] },
       { "name": "Implement schema", "prompt_file": "02_schema.md",
         "context_files": ["spec/feature.md", "src/db/index.ts"] }
     ]
   }
   ```
5. 초기화:
   ```bash
   python ~/.claude/skills/unbounded-engine/scripts/phase_runner.py init \
     --task "feat: <설명>" --phases .oneshot/phases.json
   ```

**ALWAYS: 페이즈 1은 무조건 문서 업데이트** — 스펙 드리프트 방어의 핵심.
**NEVER: 페이즈를 30개 초과로 분할** — 컨텍스트 폭발 주의.
**WHEN: 페이즈가 20개+ THEN: 작업을 2~3개 묶음으로 분할 후 순차 실행.**

### Phase B: 헤드리스 실행 (메인은 모니터링만, 컨텍스트 격리)

```bash
python ~/.claude/skills/unbounded-engine/scripts/phase_runner.py run
```

메인 세션의 역할:
- 진행 상황 모니터링 (status 명령으로 주기적 확인)
- 페이즈 실패 시 사용자에게 보고 + 재시도 vs 페이즈 분할 결정
- **절대 코드를 직접 쓰지 않는다** — 헤드리스 자식 세션이 전담

자식 세션의 역할:
- 자기 페이즈 프롬프트만 보고 작업 (메인 대화 모름)
- docs.diff 자동 주입됨 → 스펙 일치 강제
- 자가 검증 (typecheck, test) 통과 후 종료
- phase_runner.py가 자동 git commit

실패 시 복구:
- `python phase_runner.py status` — 어디서 막혔는지 확인
- 동일 페이즈 재시도: `python phase_runner.py run`
- 페이즈 자체 재설계: `python phase_runner.py reset --phase N` 후 phases/NN.md 수정 → run

### Phase C: 통합 검증 (메인 세션, 컨텍스트 여유 충분)

전 페이즈 완료 후 메인이 직접 실행 (이때 메인 컨텍스트는 20% 미만):

```
C1. 빌드: npm run build / lint / test
C2. 코드 리뷰: /review + /codex review
C3. 보안: /cso
C4. UX: /design-review + visual-proof (UI 변경 시)
C5. QA: /qa
```

BLOCK → 즉시 수정(메인에서) → C1 재실행
FAIL → 페이즈 분할 후 phase_runner reset → 재실행
최대 3회

### Phase D: 품질 게이트

harsh-critic 11항목 → PASS만 Phase E로
특히 E1(거짓완료), E5(범위누락) 통과 — docs.diff 비교로 자동 검증

### Phase E: 산출물 + 회고

- /ship 또는 PR 생성 (CodeRabbit 등 외부 리뷰 트리거)
- retrospective-engine → ALWAYS/NEVER → CLAUDE.md 누적 교훈 갱신
- progress.json 아카이브 (`.oneshot/archive/{task_name}_{date}/`)

### v2 자가 검증

- [ ] Phase A에서 코드 작성 없이 사고만 했는가?
- [ ] Phase A.5에서 페이즈 1을 **문서 업데이트**로 잡았는가?
- [ ] 모든 페이즈 프롬프트가 자기완결적인가? (메인 대화 의존 0건)
- [ ] phase_runner.py가 docs.diff를 자동 주입했는가?
- [ ] 메인 세션 컨텍스트가 Phase B 종료 후 30% 미만인가?
- [ ] 각 페이즈마다 git commit이 자동 생성되었는가?
- [ ] Phase C 통합 검증을 건너뛰지 않았는가?
- [ ] retrospective-engine 회고가 CLAUDE.md에 반영되었는가?

### v1과 동시 운영

v1("한방에 개발")은 **단순/빠른 작업** (페이즈 5개 이하)에 유지 가능.
v2("한방에 개발 v2" / "헤드리스 한방에")는 **중대규모** (페이즈 5개+)에 권장.

판별 기준:
- 페이즈 5개 이하 + 메인 세션 80% 미사용 → v1
- 페이즈 5개+ OR 메인 세션 60% 이상 사용 예상 → v2 강제

---

## 한방에 검증 모드 — 다층 교차 검증 파이프라인

> "6레이어가 교차 검증할 때 비로소 확신이 생긴다."

6축 검증을 동시 가동 + 교차 피드백 + 자동 수정.

### 트리거
"무한사고로 한방에 검증", "한방에 검증", "풀 검증", "배포 전 완전 검증"

### Wave 1: 사고 분석 (수정 없음)
제약스캔(L1/L2/L3) → 메타질문 3개 → 공격표면분석(멍거 역전)
→ 사고 브리프 (구조적 리스크 + 공격표면 지도 + 포커스 포인트)

### Wave 2: 6축 동시 검증 (사고 브리프를 컨텍스트로)
축1 코드품질: sanyuan + /review + code-analyzer
축2 보안: /cso + security-scan + /codex challenge + Shannon(가용 시)
축3 UX: /design-review + visual-proof
축4 기능: /qa + npm test + /browse
축5 성능: /benchmark + Core Web Vitals
축6 문서: CLAUDE.md vs 코드 + README vs 기능

병렬: Agent A(축1+2, opus) / Agent B(축3+4, sonnet) / 메인(축5+6+합성)

### Wave 3: 교차 피드백 + 자동 수정
합성: 사고 예측 vs 실제 발견 교차 → 적중/과잉경고/사각지대 분류
BLOCK: problem-solver Why1~3 → 수정 → 해당축 재검증 (최대 3회)
FAIL: 직접 수정 → 해당축 재검증
교차 영향: 보안수정→코드재확인 / UX수정→QA재확인

### 최종: 다층 검증 보고서
🟢배포가능 / 🟡조건부(WARN검토) / 🔴재작업 + 교훈 ALWAYS/NEVER

### 자가 검증 (한방에 검증 전용)
- [ ] Wave 1 사고 브리프를 건너뛰지 않았는가?
- [ ] 6축 중 하나라도 건너뛴 것은 없는가?
- [ ] BLOCK 0건 후에만 PASS 판정했는가?
- [ ] 수정 후 교차 영향을 재검증했는가?

---

## 모드 간 관계

```
무한조언 → (사용자 결정) → 한방에 개발 → 한방에 검증 → /ship
                 ↑         셋업(핏) ↗                     ↓
                 └──── retrospective 교훈 ─────────────────┘
```

---

## DON'T
- L3(관습적 제약)을 L2(기술적)로 오분류 — 제거 가능한 제약을 남겨두게 됨
- 메타-질문 없이 바로 해결책 — 문제 정의가 틀리면 해결책도 틀림
- 교차도메인 1개만 가져오기 — 최소 2개 이종 도메인 패턴 필요
- 멍거 감사 생략 — 확증 편향 미검출, 과적합 위험

## DO
- L3 판별이 애매하면 "누가 만든 규칙? 왜?" 질문으로 확인
- Phase 5 멍거 역전을 반드시 마지막에 실행
- 완료 시 retrospective-engine으로 교훈 기록
